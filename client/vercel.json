{
  "buildCommand": "npm ci --include=dev --prefix client && npm run build --prefix client",
  "outputDirectory": "client/dist",
  "installCommand": "npm ci --include=dev --prefix client",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
